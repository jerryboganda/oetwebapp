# Implementation Plan: OET Listening and Reading AI System v1.1

> **For the implementation agent:** Use the executing-plans workflow and complete the tasks in order. Preserve unrelated working-tree files. Use explicit git paths only; never stage `.env*`, credentials, `.codex/config.toml`, or `.superpowers/`.

**Goal:** Implement the supplied `OET_Listening_and_Reading_AI_System_Specification_v1.1.pdf` for website computer-based Listening and Reading practice/mock delivery, with deterministic server-authoritative marking, versioned conversion/policy governance, branded non-official results, grounded AI assistance, and auditable administration.

**Source of truth:** `C:/Users/Dr Faisal Maqsood PC/Downloads/OET_Listening_and_Reading_AI_System_Specification_v1.1.pdf` and the committed design at `docs/superpowers/specs/2026-08-10-oet-listening-reading-ai-system-v1-1-design.md`.

**Existing implementation anchors:** `backend/src/OetLearner.Api/Domain/ListeningEntities.cs`, `backend/src/OetLearner.Api/Domain/ReadingEntities.cs`, `backend/src/OetLearner.Api/Services/Listening/ListeningGradingService.cs`, `backend/src/OetLearner.Api/Services/Reading/ReadingGradingService.cs`, `backend/src/OetLearner.Api/Endpoints/ListeningV2Endpoints.cs`, `backend/src/OetLearner.Api/Endpoints/ReadingLearnerEndpoints.cs`, `app/listening/player/[id]/page.tsx`, `app/listening/results/[id]/page.tsx`, `app/reading/paper/[paperId]/page.tsx`, and `app/reading/paper/[paperId]/results/page.tsx`.

## Global implementation constraints

- Website computer-based delivery is in scope. Native mobile packaging and a paper-exam simulation are not acceptance substitutes.
- Marking is deterministic and server authoritative. AI may explain stored marking evidence after submission, but never scores, changes a response, alters a timer/lock, reconstructs audio, reveals answers before submission, or claims an official OET result.
- Listening contains 42 marks: Part A 24 typed responses, Part B 6 three-option MCQs, Part C 12 four-option MCQs. Reading contains 42 marks: Part A 20, Part B 6 three-option MCQs, Part C 16 four-option MCQs.
- Every attempt stores immutable test, question, answer-key, marking-policy, score-conversion, and rationale references/snapshots needed to reproduce the outcome.
- No unapproved scaled score or pass claim is emitted. Missing conversion data produces an explicit unavailable state and an admin release error; it never falls back to the current piecewise formula in `backend/src/OetLearner.Api/Services/OetScoring.cs` or `lib/scoring.ts` for Listening/Reading.
- Exact answer labels, error categories, strict-spelling disclosure, and `AI Practice Score — not an official OET result.` are treated as contract strings and covered by tests.
- Keep all user media/file I/O on the configured persistent storage abstraction; do not introduce raw filesystem storage.

## Task 1: Establish versioned assessment governance and score conversion

1. Inspect the EF model registration in `backend/src/OetLearner.Api/Data/` and the existing migration conventions. Add entities and configurations for score-conversion table versions/rows, marking-policy versions, rationale/evidence records, and controlled re-mark jobs in the existing domain/data structure. Include owner approval, effective date, immutable-after-use status, created/approved-by audit fields, and affected-attempt audit links.
2. Add migrations using the repository's normal EF migration path. Enforce unique version identifiers, one row per raw score `0..42` per table, one active/effective table per assessment/pathway scope, and no mutation of a table after it is referenced by a submitted attempt unless a formal reprocessing operation is recorded.
3. Add a score-conversion resolver service and interface under the existing backend service boundary. It must resolve by assessment (`Listening`/`Reading`), pathway/regulator scope, effective version, and raw score; return raw plus converted score plus table version; and return a typed unavailable result when no approved row exists. It must reject duplicate/missing raw rows and never interpolate.
4. Add immutable marking-policy snapshot serialization for capitalization, whitespace, explicit accepted variants, MCQ option rules, Part A form rules, timer/lock mode, and answer-key revision references. Snapshot the policy at attempt start and use the snapshot at finalization.
5. Add contract DTOs and API models for score-table version metadata, rows, policy snapshot identity, conversion-unavailable errors, and re-mark audit summaries. Keep admin write DTOs separate from candidate result DTOs so answer keys cannot cross the candidate boundary.
6. Add focused backend tests for table completeness, duplicate/missing rows, effective-version resolution, immutable-after-use behavior, conversion-unavailable behavior, and raw-score reproducibility. Add a test that confirms Listening/Reading paths do not call `OetScoring.OetRawToScaled` as a fallback.

## Task 2: Make Listening and Reading marking deterministic and revision-safe

1. In `backend/src/OetLearner.Api/Services/Listening/ListeningGradingService.cs`, replace default synonym/fuzzy/case/whitespace behavior with the owner-approved marking-policy snapshot. Trim leading/trailing spaces only; collapse internal whitespace only when enabled by the captured profile; apply capitalization only when enabled; award typed credit only for the canonical answer or explicitly published variants; award zero for blank, spelling error, synonym, extra word, wrong unit, wrong order, or wrong form; and retain variant provenance in the marking trace.
2. Preserve exact MCQ semantics: one selected option must equal the one versioned correct key; no negative marking; multiple selections or malformed selections are invalid and flagged for review; elimination/strikethrough metadata never becomes a selection.
3. In `backend/src/OetLearner.Api/Services/Reading/ReadingGradingService.cs`, enforce strict Part A response rules from the snapshot, including exact matching/short-answer/completion behavior, explicit accepted variants only, and the configured no-partial-credit rule. Emit the exact error category `incorrect form` where a response is semantically related but violates the required answer form, such as a range where a maximum is required.
4. Add immutable question/key revision selection to both grading services. A submitted attempt must grade against the revision snapshot recorded at start/finalization, not a mutable live question row. Include canonical answer, published variants, MCQ option IDs, part/type, and rationale/source reference in the trace.
5. Replace Listening/Reading formula conversion calls with the versioned resolver from Task 1. Keep raw score authoritative even when conversion is unavailable, and expose the unavailable state through result contracts without inventing a pass/fail outcome.
6. Implement controlled re-marking for an answer-key or explicit-variant correction only. Record original key/variant, new key/variant, reason, requester, approver, timestamp, affected candidates, original result, new result, and resulting score-table/policy versions. Make re-mark idempotent and prevent arbitrary candidate-response edits.
7. Update backend tests under `backend/tests/OetLearner.Api.Tests/Listening/` and `backend/tests/OetLearner.Api.Tests/Reading/` for exact spelling, explicit variants, whitespace/capitalization profiles, malformed MCQ selection, no fuzzy/synonym credit, incorrect-form labeling, revision reproducibility, score conversion, and controlled re-mark audit behavior.

## Task 3: Enforce server-authoritative delivery, timers, locks, and audio integrity

1. Audit `backend/src/OetLearner.Api/Services/Listening/ListeningSessionService.cs`, `backend/src/OetLearner.Api/Endpoints/ListeningV2Endpoints.cs`, `backend/src/OetLearner.Api/Endpoints/ListeningLearnerEndpoints.cs`, and the Listening attempt entities. Add authoritative start/transition/finalize state validation, irreversible section locks, server-clock deadline calculation, reconnect state restoration, encrypted autosave metadata, and idempotent submit handling. Refresh must restore the same attempt and never create extra time or a second attempt.
2. Preserve Listening A1→A2→B→C1→C2 sequencing. Require a server-validated boundary confirmation, warn about unanswered items, record transition/focus/buffering/playback events, and reject post-lock writes. Ensure a verified platform audio failure creates an admin-visible fault state and does not silently grant replay.
3. Audit `backend/src/OetLearner.Api/Endpoints/ListeningAudioEndpoints.cs`, `components/domain/listening/player/ListeningAudioTransport.tsx`, and `app/listening/player/[id]/page.tsx`. Keep the audio integrity/preload gate, one-play/seek/pause/replay restrictions for the selected lock mode, no download/media-control escape, authorized playback capability, and playback/buffering/focus-loss telemetry. Apply the approved practice/mock lock setting through the attempt policy snapshot.
4. Audit `backend/src/OetLearner.Api/Services/Reading/ReadingAttemptService.cs`, `backend/src/OetLearner.Api/Endpoints/ReadingLearnerEndpoints.cs`, and `app/reading/paper/[paperId]/page.tsx`. Enforce exactly 15 authoritative minutes for Reading Part A, then a shared server-authoritative 45-minute B+C budget, with no client-side extension on reload/reconnect. Persist answers and position, lock Part A on expiry/transition, and make finalization idempotent.
5. Verify candidate payloads do not include answer keys, accepted variants, rationales, or future-section answers before finalization. Ensure result/review endpoints require a finalized attempt and return only the candidate's own data, with tutor/admin scopes enforced server-side.
6. Add focused backend tests for section transitions, lock rejection, timer drift, reconnect/no-extra-time, duplicate submit, answer-key secrecy, audio fault handling, Part A lock, shared B+C deadline, and cross-candidate authorization. Extend the existing Playwright checks `tests/e2e/listening/exam-mode-locks.spec.ts`, `tests/e2e/listening/listening-answer-key-not-exposed.spec.ts`, and `tests/e2e/reading/part-a-lock.spec.ts` only where the new contract requires coverage.

## Task 4: Deliver compliant results, review, graph, and disclosure UI

1. Extend result contracts and presenters used by `app/listening/results/[id]/page.tsx` and `app/reading/paper/[paperId]/results/page.tsx` with raw score, converted score/unavailable state, conversion-table version, part breakdown, correct/incorrect/unanswered counts, marking trace, source sentence/rationale references, MCQ option review, error category, pattern summary, and targeted study links. Do not expose answer/rationale content before finalization.
2. Add a reusable Listening/Reading score-band graph component under the existing component domain. Use the platform palette/typography, make it visually distinct from the official OET green result strip, show raw and converted values plus conversion-table version, and render the exact persistent label `AI Practice Score — not an official OET result.`. Do not reuse the existing accuracy gauge as the only score visualization.
3. Add the exact strict-marking disclosure to Listening results: `This platform grades minor spelling variations strictly to build exam-safe habits — some real OET examiners may allow minor variants at their discretion.` Keep the result language estimate/practice-only and remove any unsupported official pass implication.
4. Implement full item review: typed response, correct answer, error category, source sentence, stored rationale; MCQ selected option, correct option, and author-approved explanation. Include unanswered items and avoid hallucinated rationale text when stored evidence is missing.
5. Implement responsive desktop/mobile layouts for simultaneous or stacked Reading passage/question presentation, answer-position restoration, keyboard/focus semantics, timer announcements, zoom without clipping, and score/review layout at narrow widths. Preserve existing app-shell, theme, and routing contracts.
6. Add frontend unit tests for score graph rendering, exact disclosure text, conversion-unavailable state, full review fields, no pre-submit answer leakage, responsive state/position preservation, and accessibility labels. Add targeted Playwright assertions for mobile/desktop no-clipping and final-result-only answer review.

## Task 5: Add grounded post-submit AI explanations and analytics

1. Locate the existing grounded AI gateway, usage-recording service, rationale/evidence contracts, and study-plan components referenced by `docs/AI-USAGE-POLICY.md`. Add Listening/Reading result explanation requests that accept only stored marking traces, source sentences, author-approved rationales, evidence counts, and the candidate's finalized response.
2. Enforce server-side post-submit authorization and grounding. The AI endpoint must refuse pre-submit, cross-candidate, missing-evidence, or unsupported-official-outcome requests; every successful or failed call must create the existing usage/audit record; and an AI outage must leave the deterministic result immediately available.
3. Add editable study-plan recommendations generated from error patterns and linked to existing drills/content. Persist candidate edits separately from the generated recommendation and do not allow the AI to alter scores, keys, timers, entitlements, or official-result claims.
4. Add backend tests for evidence-only prompt construction, unsupported-claim rejection, usage-row creation, authorization, empty-rationale behavior, and AI-failure/nonblocking result delivery. Add frontend tests for retry/error rendering without a hard reload and editable-plan persistence.

## Task 6: Complete admin authoring, validation, roles, and privacy controls

1. Load `.github/instructions/admin-hallmark.instructions.md` before editing admin surfaces. Extend the existing Listening/Reading admin routes under `app/admin/content/` and backend authoring endpoints so every published question requires part, type, correct answer, mark, status, valid option counts, unique option values, explicit variants with who/when/why, audio duration/timing validation, source/rationale evidence, candidate preview, and marking preview.
2. Add admin score-conversion table management at `app/admin/content/scoring-system/page.tsx` and its backend endpoint boundary. Support draft/review/approval/effective/locked states, complete 0..42 rows, pathway/regulator scope, date-effective versioning, preview, immutable-after-use rejection, and release blocking when owner data is absent. Do not seed fabricated conversion values.
3. Add admin marking-policy and rationale/evidence management to the existing Listening/Reading policy/content tools. Require explicit approvals for normalization/spacing/capitalization, practice/mock audio locks, rationale text, pass thresholds, technical-guidance mode, and score-graph legal/style status. Surface unapproved inputs as blocking release status.
4. Add controlled re-mark job review screens and audit views. Restrict re-mark triggers to key/explicit-variant corrections, show affected candidates, preserve old/new outputs, and require the configured approver role before execution.
5. Map the PDF roles to existing authorization policies: Candidate own attempts/results only; Tutor assigned candidates only with comment/override audit; Content Author no candidate PII; Clinical Reviewer and Language Assessor limited to their review surfaces; Admin full audit; Customer Support ticket-linked, time-limited access. Add endpoint authorization tests for each boundary and ensure candidate PII is excluded from authoring APIs.
6. Add privacy controls for encrypted attempts/results, retention/deletion requests, candidate export, restricted admin audit access, and export audit records using existing privacy/security services. Confirm logs never contain answer keys, credentials, raw tokens, or unnecessary candidate data.

## Task 7: Verify the full acceptance matrix and ship safely

1. Create a compact acceptance evidence document mapping LR-01 through LR-16 to exact backend tests, frontend tests, Playwright checks, and live route/health evidence. Include separate evidence for raw reproducibility, conversion-table version, AI-failure nonblocking behavior, re-mark audit, and authorization/privacy.
2. Run the smallest touched-area host validation required by the repository workflow after implementation, then run only additional focused checks needed for failed or high-risk acceptance gates. Do not run a full multi-suite marathon or build on the VPS. Record commands and outcomes in `.github/agent-state.local.md` and keep `PROGRESS.md` compact.
3. Inspect the final diff and staged file list for unrelated changes, secrets, `.env*`, generated artifacts, and accidental edits to `.codex/config.toml` or `.superpowers/`. Stage explicit implementation paths only.
4. Commit the implementation with a focused message, push `main`, and verify the GitHub Actions deployment run for the pushed SHA. Confirm production web/API health and the deployed result routes; report any owner-controlled release gate that remains unverified, especially missing approved score-conversion rows, thresholds, rationale approvals, legal graph sign-off, or load/concurrency evidence.

## Required owner-controlled release inputs

Implementation must fail closed until the owner supplies or approves the complete 0..42 Listening and Reading conversion tables, normalization/spacing/capitalization profile, practice/mock audio lock mode, rationale/evidence library, pathway/regulator thresholds, technical-guidance-only decision, score-graph legal/style sign-off, and peak concurrent timed-attempt target. These values must be stored through the governed admin workflow, never invented in code or fixtures.

## Definition of done

The feature is complete only when deterministic raw marking and versioned audit behavior pass, the 16 PDF acceptance gates have evidence, candidate/tutor/admin authorization boundaries are proven, result graphs/disclosures/reviews are visible and responsive, AI is grounded and nonblocking, the approved score tables and other release inputs are configured, `main` is pushed, the production deployment for that SHA completes, and production web/API health plus the relevant Listening/Reading routes are verified. A passing local build, a healthy container, or a pushed commit alone is not completion.
