# Desktop App Issue Register

| Issue ID | Title | Severity | Affected area | Reproduction steps | Observed behavior | Expected behavior | Root cause | Files changed | Fix applied | Validation result | Regression notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DSK-001 | Packaged desktop shell timed out waiting for backend readiness | Critical | Packaged Windows startup, installed desktop shell | 1. Launch installed `OET Prep.exe` 2. Wait for startup 3. Observe error dialog | Electron showed `OET Prep failed to start` after waiting on `http://127.0.0.1:5199/health/ready` | Installed desktop should launch its bundled backend and open normally | SQLite-backed packaged runtime hit an EF Core translation failure in background job startup because `DateTimeOffset` ordering was pushed into SQLite SQL | `backend/src/OetLearner.Api/Services/BackgroundJobProcessor.cs` | Added provider-aware queued-job loading so SQLite materializes a bounded set and orders due jobs in memory instead of unsupported SQL | Resolved. Packaged backend returned `200 OK` from `/health/ready`; installed shell launched successfully | Low. Future packaged SQLite queries that sort on `DateTimeOffset` remain a regression class |
| DSK-002 | Local Windows packaging needed an explicit unsigned QA path | High | Packaging and release validation | 1. Build desktop package locally on Windows without signing material 2. Attempt local QA install | Local packaging was ambiguous for QA without a clear unsigned-build path | Local QA should support a documented unsigned build flow without pretending release signing is configured | Windows packaging requires either real signing credentials or an explicit QA override | `scripts/desktop-dist.cjs`, `electron-builder.config.cjs` | Verified and documented local QA path with `ELECTRON_ALLOW_UNSIGNED_WINDOWS_BUILD=true` | Resolved for local QA | Medium for real release readiness because signing is still an external requirement |
| DSK-003 | Desktop dev shell depends on the Docker-backed local stack | Medium | Dev startup, local environment | 1. Run `npm run desktop:dev` without healthy local services 2. Observe startup failure | Dev shell cannot run if renderer and backend are not already healthy | Dev shell should launch only against a known-good local baseline | Electron dev mode intentionally targets the Docker-backed renderer on `localhost:3000` and API on `localhost:5198` | `scripts/electron-dev.cjs`, `scripts/qa/assert-local-stack.mjs`, `docs/qa/desktop_app_run_guide_verified.md` | Kept health-gated startup and documented the verified prerequisite checks | Resolved as a documentation and workflow issue | Low if the documented local stack contract is preserved |
| DSK-004 | Desktop Playwright smoke tests acted before the Electron UI had actually painted | High | Desktop QA harness, dev-shell tests | 1. Run `npx playwright test -c playwright.desktop.config.ts tests/e2e/desktop/electron-smoke.spec.ts --reporter=line` 2. Observe early `page.goto()` timeouts in authenticated tests | `firstWindow()` plus `domcontentloaded` returned while the window body was still visually blank, so tests hydrated session state and navigated too early | Desktop tests should only interact after the renderer has painted visible UI | Electron window acquisition waited only for a low-level document event, not for real rendered UI content | `tests/e2e/desktop/electron-smoke.spec.ts`, `tests/e2e/desktop/electron-packaged-smoke.spec.ts`, `tests/e2e/desktop/electron-surface-validation.spec.ts` | Added a rendered-body readiness poll to `getDesktopPage()` so desktop tests wait for visible UI before the first interaction | Resolved. Smoke suite and full desktop suite now pass reliably in one serial run | Low. Any future desktop tests should keep using the rendered-UI readiness helper |
| DSK-005 | Windows Electron cleanup was incomplete for Playwright-launched process trees | High | Desktop QA harness, teardown on Windows | 1. Run longer desktop suites repeatedly 2. Inspect process list after failures | Leaked `electron.exe` processes caused flaky later runs and teardown instability | Desktop test cleanup should remove the full Playwright Electron process tree between tests | Previous cleanup only matched command lines or root processes and missed some Windows Electron descendants | `tests/e2e/desktop/electron-smoke.spec.ts`, `tests/e2e/desktop/electron-packaged-smoke.spec.ts`, `tests/e2e/desktop/electron-surface-validation.spec.ts` | Added explicit Windows `taskkill /T /F` process-tree cleanup in desktop teardown | Resolved. No leaked Playwright Electron processes remained after the final clean suite run | Low after retest |
| DSK-006 | Notification hub reconnect noise produced false-negative desktop E2E failures | Medium | Desktop QA diagnostics during learner, expert, and admin route transitions | 1. Run extended desktop validation flows 2. Observe SignalR reconnect failures during route transitions and review submission | Tests failed on notification long-poll reconnect console errors or `404` responses even when user flows succeeded | Desktop validation should fail only on real product defects, not benign route-transition reconnect noise | The notification center issues long-poll reconnect attempts that can fail during navigation/teardown in Electron validation, producing harmless console noise and occasional `404` responses | `tests/e2e/fixtures/diagnostics.ts`, `tests/e2e/desktop/electron-surface-validation.spec.ts` | Extended diagnostics to classify the specific notification reconnect patterns as ignorable only when explicitly enabled per test | Resolved. Full surface validation suite passed with scoped allowances on affected flows | Low. The reconnect behavior is still noisy, but it is now clearly classified and bounded in the harness |
| DSK-007 | Speaking role-card toggle overlapped primary recording controls in desktop viewport | Medium | Learner speaking task UI | 1. Open `/speaking/task/st-001?mode=self` in desktop shell 2. Start recording 3. Inspect floating controls | Role-card toggle cluster sat too close to the record/submit control area | Speaking controls should remain fully accessible with no overlap in the desktop viewport | The absolute-positioned role-card/notes toggle sat too high in the speaking task layout | `app/speaking/task/[id]/page.tsx` | Moved the floating toggle cluster from `bottom-32` to `bottom-8` | Resolved. Learner immersive speaking flow passed in the desktop shell after rebuild/retest | Low |
| DSK-008 | Packaged desktop sign-in rejected valid web credentials because it used a different auth database | Critical | Packaged desktop authentication | 1. Build and launch the packaged desktop shell without a shared API override 2. Attempt sign-in with a real web-app account 3. Compare with the same credentials on the web app | Desktop returned `Invalid email or password` even though the same credentials worked on the web app | Packaged desktop should authenticate against the same configured user store as the web app whenever a shared API is configured | Packaged Electron startup always booted a bundled ASP.NET Core backend backed by a private SQLite database seeded with local demo accounts, so desktop auth could point at a different user store than the web deployment | `electron/main.cjs`, `electron-builder.config.cjs`, `electron/runtime-config.cjs`, `electron/__tests__/runtime-config.test.ts` | Added packaged runtime API selection so Electron uses a configured shared API when one is provided, and only falls back to the bundled SQLite backend when no real API is configured. Added build-time persistence of the packaged API target plus unit coverage for override and fallback behavior | Resolved in code and verified by targeted unit tests plus production build. Packaged desktop now has an explicit auth-parity path with the web app when `PUBLIC_API_BASE_URL`, `API_PROXY_TARGET_URL`, or an absolute `NEXT_PUBLIC_API_BASE_URL` is configured at build/runtime | Low for configured production builds; medium if packaged apps continue to be built without a shared API URL and users expect web-account parity |
