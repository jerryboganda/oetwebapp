'use client';

// TODO Phase 1: modal that appears when a RequireAdmin tool fires an
// ApprovalRequest frame. Shows tool name, args, diff (for write_file),
// command preview (for run_command), and approve/reject buttons.
// On approve/reject -> aiAssistantClient.approveToolInvocation.
// TODO: write AuditEvent (server side).
export function ApprovalModal(): React.JSX.Element | null {
  return null;
}
