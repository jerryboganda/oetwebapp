# Learner Mobile Issue Register

| Issue ID | Title | Severity | Affected area | Reproduction steps | Observed behavior | Expected behavior | Root cause | Files changed | Fix applied | Validation result | Regression notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| LMB-001 | Capacitor web root was missing a native entry page | High | Capacitor bootstrap, Android/iOS scaffold generation | 1. Point Capacitor at the app web output 2. Run the native add/sync flow 3. Observe scaffold generation | Capacitor could not scaffold the native app cleanly because the expected web entry was not present | Native project generation should succeed with a stable entry page | The shell pointed at the normal web output instead of a dedicated Capacitor web root | `capacitor.config.ts`, `capacitor-web/index.html` | Added a dedicated `capacitor-web` entry page and pointed Capacitor at it | Resolved. Android and iOS add/sync completed successfully | Keep the placeholder web root in place if the Capacitor web directory changes again |
| LMB-007 | Capacitor could ship the scaffold page when no public origin was configured | Critical | Capacitor packaging, Android/iOS runtime bootstrap | 1. Leave `APP_URL` and `CAPACITOR_APP_URL` unset 2. Package the mobile app 3. Install and launch the APK | The installed app opened the local scaffold page instead of the deployed Next.js origin | Installed app should load the live Next.js origin or use the documented fallback host instead of the scaffold page | Capacitor config only read a manual env override and could silently fall back to the scaffold root | `capacitor.config.ts`, `lib/mobile/capacitor-config.ts`, `capacitor-web/index.html` | Capacitor now resolves `APP_URL` or `CAPACITOR_APP_URL`, and falls back to `https://app.example.com` when the workspace env is still a template | Focused helper tests passed; the build still needs a real deployment URL for non-sample environments | Keep the deployed origin in sync with the packaging environment |
| LMB-002 | TypeScript deprecation config blocked clean builds | Medium | Frontend build | 1. Run the production build 2. Observe the TypeScript configuration warning/error | Build-time TypeScript checks complained about an unsupported deprecation setting | The build should complete without configuration errors | The TypeScript config used an unsupported deprecation setting value | `tsconfig.json` | Corrected the setting to a supported value | Resolved. `cmd /c npm run build` passed | Re-check this during future TypeScript upgrades |
| LMB-003 | Speaking task recording was browser-only on mobile | Critical | Learner speaking task | 1. Open the speaking task in the Capacitor shell 2. Start recording 3. Try to submit audio | Mobile recording depended on browser APIs that are unreliable in a webview | The speaking task should record reliably on device | The page only used the browser media path before the native bridge existed | `app/speaking/task/[id]/page.tsx`, `lib/mobile/speaking-recorder.ts`, `android/app/src/main/java/com/oetprep/learner/plugins/SpeakingRecorderPlugin.java`, `ios/App/App/SpeakingRecorderPlugin.swift` | Added a native speaking recorder bridge and routed Capacitor devices through it | Resolved in code. Browser fallback remains intact for web | Physical device QA is still required for microphone permission and audio-capture confidence |
| LMB-004 | Diagnostic speaking lacked a native fallback path | High | Learner diagnostic speaking flow | 1. Open the diagnostic speaking page in the native shell 2. Check permission and recording behavior | Diagnostic speaking stayed tied to the browser path | Diagnostic speaking should validate microphone behavior on native devices too | The diagnostic page had no Capacitor branch | `app/diagnostic/speaking/page.tsx` | Added native permission and recording fallback using the speaking recorder bridge | Resolved. File-level validation passed and build remained green | Keep the diagnostic path aligned with the main speaking task flow |
| LMB-005 | Speaking readiness check could not confirm native microphone readiness | High | Speaking check flow | 1. Open the speaking check page in the native shell 2. Attempt the microphone check and sample recording | Permission and sample-recording checks were incomplete for native devices | The check page should confirm readiness before the user enters the live task | The flow lacked native permission handling and stream cleanup | `app/speaking/check/page.tsx` | Added native fallback plus explicit stream cleanup and timeout handling | Resolved. File-level validation passed and build remained green | This should be rechecked on real Android/iPhone hardware |
| LMB-006 | Speaking task cleanup produced a hook stability warning | Medium | Speaking task component lifecycle | 1. Run lint/build after the recorder integration 2. Inspect the React hook warning | Cleanup helpers were recreated often enough to trigger a hook stability warning | Cleanup logic should be stable and warning-free | Cleanup helpers were not stable callbacks | `app/speaking/task/[id]/page.tsx` | Converted cleanup helpers to stable callbacks | Resolved. Final production build succeeded after the change | Low risk if future lifecycle helpers preserve the same pattern |

## Summary

- The remaining risks are validation gaps, not known code defects.
- The highest-risk path was speaking capture, and it now uses a native implementation on Capacitor devices.
