node_modules/
.next/
coverage/
.DS_Store
*.log
.tmp-*
.playwright-cli/
.playwright/
.playwright-mcp/
playwright/.auth/
playwright-report-prod/
dist/
desktop-backend-runtime/
output/
output/playwright/
.tmp-*.png
.agents/
.claude/
.github/context-mode/
.github/context-archive-*/
.github/agent-state.local.md
.kilo/*
!.kilo/command/
!.kilo/command/**
!.kilo/agent/
!.kilo/agent/**
!.kilo/skills/
!.kilo/skills/**
backend/**/bin/
backend/**/obj/
backend/src/OetLearner.Api/App_Data/
backend/oet_learner_dev.sqlite
backend/src/OetLearner.Api/oet_learner_dev.db
backend/src/OetLearner.Api/oet_learner_dev.db-shm
backend/src/OetLearner.Api/oet_learner_dev.db-wal
backend/src/OetLearner.Api/Data/Migrations/__PendingProbe/
**/bin/
**/obj/
build-*.txt
server*.log
dev*.log
dev-output.txt
# Validation / CI scratch (Docker build logs, generated diffs, vitest dumps)
_ws_*.txt
backend/_ws_*.txt
_gh_*.txt
_tsc_*.txt
_gen_*.diff
_review_focus.diff
vitest-output.txt
vitest-verbose-output.txt
tsconfig.tsbuildinfo
.env*
!.env.example
!.env.staging.example
!.env.docker-local.example

# Local/temp artifacts
.local-deploy/
.local-docker/
.deploy/
.opencode/*.tmp
Contactppp.png
Contactzzz.png
$sdkRoot/
_previous_chat_history/
dmb_*.py
n u l  

# Local test/build/audit output (never commit)
nul
*_out.txt
*_out2.txt
*_output.txt
build_output.txt
build_result.txt
npm_test_out.txt
backend_test_out.txt
backend_test_out2.txt
vitest_*.json
vitest_*.txt
vitest_output*
vitest_results_tmp.json
vitest_baseline.json
vitest_check.txt
vitest_full_run.txt
audit-*.png
audit-*.md
audit-*.snapshot.md
fix_routes.py

# Content / vendor drops that should not be in VCS
Project Real Content/
vscode-digitalocean-model-provider/

# Rulebook extraction scratch area (regenerated from PDFs on demand)
_extracted/
_extract_*.py
_check_pdf_libs.py

# oh-my-opencode cross-session task state
.sisyphus/
.opencode/*.bak-pre-*

ts-prune-survivors.txt

# CommandCode IDE local cache
.commandcode/


backend/tests/**/TestResults/
test-full.log
front-test.log

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Repomix bundles (generated)
repomix-output.*
repomix-output*.xml
repomix-output*.md
repomix-output*.json
repomix-output*.txt

# Storybook
storybook-static/

# Local-only working artifacts
copilot-worktrees/
t.err

# Local-only audit reports (generated by tooling, never committed)
_audit/

# Root-level transient scratch exports (audio mp3 dumps, SQL/CSV data exports)
/tmp-*

# Build verification scratch dirs
bin-verify*/
**/bin-verify*/


# QA audit artifacts (transient — scripts only are tracked)
.qa-artifacts/*
!.qa-artifacts/scripts/
!.qa-artifacts/scripts/**
.qa/
.qa-audit/

# Root-level transient Playwright screenshots (committed copies live under docs/)
/verify-*.jpeg
/verify-*.png
/axelit-*.png
/axelit-*.jpeg

# VPS operations scripts (never commit — contain server paths/credentials)
_vps_*.sh
/check.sh

# One-off ops/admin scripts (underscore-prefixed scratch)
scripts/_*
scripts/admin/_*
scripts/admin/__pycache__/
scripts/*.sql
scripts/private-*.sql

# Root-level development screenshots
/*.png
/*.jpg
/*.jpeg

# One-off architecture/audit/progress docs (not canonical project docs)
/mission-critical-*.md
/OET_AI_ARCHITECTURE*.md
/OET_AUDIT*.md
/security-analysis*.md
/skills-lock.json
speaking markdown*.md
VPS All in One File.md

# OET sample content (store externally, not in repo)
Listening (*/
Reading (*/
OET/
OET Materials & Videos Data/
OET with Dr. Ahmed Hesham ( Medicine Only )/

# Database backup dumps
*.sql.bak
*.dump
.db-sync/
oet_learner_dev_backup_*.sql

# Production DB sync (contains real production data/PII — NEVER commit)
.db-sync/
*.dump
*.dump.gpg

# Misc scratch
*.bat
_pwhash*
_cols.sql
_create_users.sql
__pycache__/
*.pyc

# Linked git worktrees (separate checkouts — never commit)
.worktrees/

# Tauri desktop shell
src-tauri/target/
src-tauri/binaries/
src-tauri/tauri.build.conf.json
src-tauri/*.log
src-tauri/cdp-*.mjs
src-tauri/cdp-realtime-result.json
src-tauri/*-result.json

# --- added 2026-06-20: keep prod working-tree debris out of git ---
.pnpm-store/
/_*.sh
/_*.sql
*.bak-*

# --- Code-signing / updater key material — NEVER commit (QA prod-readiness) ---
# Authenticode certs, PKCS#12 bundles, PEM/DER keys, strong-name keys, and the
# Tauri minisign updater private key. Public certs (.cer) shared with testers are
# distributed out-of-band, not via the repo.
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt
*.snk
# Tauri signer output / local key store
.tauri/
src-tauri/*.key
src-tauri/*.pub

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# Durable inputs stay tracked: .design-sync/{config.json,NOTES.md,conventions.md,
# previews/,overrides/,build/tw-input.css,build/compile-css.mjs}
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/build/styles.compiled.css

# Bulk video upload scratch (state, manifest, logs) — never commit
scripts/videos/state/

# Materials ingest scratch (state, manifest, logs) — never commit
scripts/materials/state/

# CodeGraph index (per-environment artifact — never commit)
.codegraph/

# Serena local project index/cache/memories (per-environment artifact — never commit)
.serena/
