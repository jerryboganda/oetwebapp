{
  "$schema": "https://schema.tauri.app/config/2",
  "bundle": {
    "resources": {
      "../.next/standalone": "standalone",
      "../desktop-backend-runtime": "backend-runtime",
      "binaries/node.exe": "node.exe"
    }
  }
}
