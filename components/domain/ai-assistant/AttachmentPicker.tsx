'use client';

// TODO Phase 1: file picker that browses the repo via
// // TODO: via IFileStorage or ICodebaseExecutor (sandboxed)
// using SearchCodebaseTool + ListDirectoryTool endpoints.
export function AttachmentPicker(): React.JSX.Element {
  return (
    <div className="p-3 text-sm text-slate-500" data-testid="ai-assistant-attachment-picker">
      Attachment picker (not implemented)
    </div>
  );
}
