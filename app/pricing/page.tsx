// OET 2026 catalog landing page — same surface as /catalog so prospects
// can hit either URL. Per the implementation plan (Wave 4.1), /pricing is
// the canonical public matrix that mirrors the PDF "Pricing matrix at a
// glance" layout.
export { default } from '../catalog/page';
