{
  "name": "@oet/lib",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript libraries for the OET with Dr Hesham Platform frontend",
  "scripts": {
    "build": "cd .. && pnpm exec tsc --noEmit",
    "test": "cd .. && pnpm test"
  }
}
