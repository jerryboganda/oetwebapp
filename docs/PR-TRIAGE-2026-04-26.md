# PR Triage - 2026-04-26

Scope: local/GitHub-only review notes for open PRs #2, #3, and #4. No production deployment was performed.

## PR #2 - `fix(auth): add missing migration for admin RBAC tables (unblocks admin sign-in)`

Decision: **request changes before merge**.

Evidence:

- PR is open and not draft.
- Diff touches migrations and deployment scripts; migration/script changes require a fresh rebase against current main and green CI before merge.
- `qa-smoke` was failing at review time.

Required changes:

1. Rebase onto current `main` after the local mission-critical branch lands.
2. Re-run CI and backend migration tests.
3. Keep deployment scripts local-safe and document rollback expectations.
4. Re-request review when checks are green.

## PR #3 - `feat(study-planner): admin-managed Study Planner v2 (end-to-end)`

Decision: **keep draft / request changes**.

Evidence:

- PR is draft.
- CI was failing at review time.
- The body explicitly says not to deploy to production yet.
- Large end-to-end feature scope should not merge while current cleanup/security/conversation work is in flight.

Required changes:

1. Keep as draft until CI is green.
2. Rebase after this branch lands.
3. Provide acceptance evidence and targeted tests for admin/learner study-plan flows.

## PR #4 - `feat(progress): admin-managed Progress v2 dashboard (end-to-end)`

Decision: **request split and rebase before review**.

Evidence:

- PR is draft.
- CI was failing and the PR was reported as conflicting.
- Diff was too large for normal review tooling (`gh pr diff` exceeded the reviewable response size).

Required changes:

1. Split into smaller PRs by backend contracts, admin authoring, learner UI, and tests.
2. Rebase on current `main`.
3. Ensure each split PR has green CI and narrow acceptance criteria.
