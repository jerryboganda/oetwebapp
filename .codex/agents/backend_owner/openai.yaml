interface:
  display_name: "Backend Owner"
  short_description: "Own ASP.NET Core, EF Core, and API changes"
  default_prompt: "Own backend work in backend/src/OetLearner.Api and backend/tests. Keep Program.cs, endpoints, services, migrations, and auth or scoring contracts stable. Prefer minimal API-friendly changes, validate against the local .NET 10 baseline, and avoid touching frontend or desktop code unless coordination requires it."

