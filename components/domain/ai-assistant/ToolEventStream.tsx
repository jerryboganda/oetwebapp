'use client';

// TODO Phase 1: render collapsible cards for each ToolCallStart / Delta /
// Result frame, with diff preview for write_file and stdout/stderr for
// run_command.
export function ToolEventStream(): React.JSX.Element | null {
  return null;
}
